# Open-core pubspec — used by the GitLab→GitHub mirror pipeline.
# Replaces the root pubspec.yaml in the public snapshot (pure Dart, no Flutter).
name: genaisys
description: "Genaisys — open-core orchestration engine for AI-assisted software delivery."
publish_to: 'none'
version: 0.0.4

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  meta: ^1.16.0
  yaml: ^3.1.2
  yaml_edit: ^2.2.1
  freezed_annotation: ^3.1.0
  json_annotation: ^4.10.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
  build_runner: ^2.10.5
  freezed: ^3.2.5
  json_serializable: ^6.12.0
